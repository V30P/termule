{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build with SaddleBag",
            "type": "shell",
            "command": "__BUILD_SCRIPT_PATH__",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "options": {
                "shell": {
                    "executable": "/bin/bash",
                },
                "cwd": "${workspaceFolder}"
            }
        }
    ]
}